/**
 * Data Models
 * TypeScript interfaces matching database schema
 */

// Re-export types from types/index.ts for convenience
export * from '../types';

// Additional model-specific types and helpers can go here
