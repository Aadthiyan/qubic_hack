// Re-export backend types for frontend use
export type {
    Project,
    ProjectMetadata,
    Score,
    ScoreBreakdown,
    ScoreResult,
    RiskFlag,
    LaunchConfig,
    SimulateRequest,
    AnalyticsResponse,
} from '../../../backend/src/types';
